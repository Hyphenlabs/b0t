{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "buildCommand": "rm -rf node_modules/.cache 2>/dev/null || true && NPM_CONFIG_PRODUCTION=false npm ci --omit=dev && npm run build && cp -r public .next/standalone/ && cp -r .next/static .next/standalone/.next/"
  },
  "deploy": {
    "preDeployCommand": ["npm run db:push:force"],
    "startCommand": "HOSTNAME=0.0.0.0 node .next/standalone/server.js",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
